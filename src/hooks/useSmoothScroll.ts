"use client";

/**
 * Hook for smooth scrolling.
 * Disabled Lenis to prevent scroll lagging and stopping on desktop/mobile browsers.
 * Replaced with lightweight CSS scroll-behavior: smooth.
 */
export function useSmoothScroll() {
  // No-op to restore native browser scrolling speeds and physics
}
